import React from 'react';

const DropdownSelectedIcon = ({
  children,
}: {
  children: JSX.Element | JSX.Element[];
}) => {
  return <>{children}</>;
};

DropdownSelectedIcon.displayName = 'Dropdown.SelectedIcon';

export default DropdownSelectedIcon;
