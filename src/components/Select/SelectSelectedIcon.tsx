import React from 'react';

const SelectSelectedIcon = ({
  children,
}: {
  children: JSX.Element | JSX.Element[];
}) => {
  return <>{children}</>;
};

SelectSelectedIcon.displayName = 'Select.SelectedIcon';

export default SelectSelectedIcon;
